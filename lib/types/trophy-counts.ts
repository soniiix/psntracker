export interface TrophyCounts {
    /** Total count of bronze trophies. */
    bronze: number;

    /** Total count of silver trophies. */
    silver: number;

    /** Total count of gold trophies. */
    gold: number;

    /** Total count of platinum trophies. */
    platinum: number;
}